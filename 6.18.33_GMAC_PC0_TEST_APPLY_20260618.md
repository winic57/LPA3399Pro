# 6.18.33 GMAC reset GPIO PC0 测试应用记录

日期: 2026-06-18
目的: 按 TB-RK3399ProD 参考，单独测试 `snps,reset-gpio = gpio3 PC0 active-low`。

## 新 DTB

```text
/mnt/sdboot/dtb/rockchip/gmac-pc0-test.dtb
```

来源：

```text
/mnt/sdboot/dtb/rockchip/gmac-test.dtb
```

## 单变量修改

```text
snps,reset-gpio = <gpio3 0x10 active-low>
```

即：

```text
fdtget -t x ... snps,reset-gpio
23 10 1
```

保留：

```text
rx_delay = 0x11
tx_delay = 0x28
status = okay
```

## extlinux

已切换为：

```text
FDT /dtb/rockchip/gmac-pc0-test.dtb
```

备份：

```text
/mnt/sdboot/extlinux/extlinux.conf.gmac-pc0-test.bak
```
