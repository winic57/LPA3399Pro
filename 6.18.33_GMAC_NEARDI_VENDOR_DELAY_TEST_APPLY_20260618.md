# 6.18.33 GMAC neardi vendor delay 测试应用记录

日期: 2026-06-18
目的: 按 LPA3399Pro vendor DTS `rk3399pro-neardi-linux-ld110-phy.dtsi` 的原厂参数测试 GMAC。

## 新 DTB

```text
/mnt/sdboot/dtb/rockchip/gmac-neardi-vendor-delay-test.dtb
```

来源：

```text
/mnt/sdboot/dtb/rockchip/gmac-test.dtb
```

## 调整为 vendor neardi 参数

```text
clock_in_out = input
snps,reset-gpio = <gpio3 PB7 GPIO_ACTIVE_LOW>
tx_delay = 0x21
rx_delay = 0x15
```

验证：

```text
tx=21
rx=15
clock=input
reset=23 f 1
```

## extlinux

已切换：

```text
FDT /dtb/rockchip/gmac-neardi-vendor-delay-test.dtb
```
