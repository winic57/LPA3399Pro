# 6.18.33 boot_20260618_093418 后续修改记录

> 时间：2026-06-18 20260618_093828

## 1. 结论

`maxcpus=4` 后系统已稳定推进到 multi-user/network/ssh 阶段，无 kernel panic。剩余问题集中在串口控制台登录与 plymouth/getty/device unit。

## 2. 本次已执行修改

### boot 参数

已在 `extlinux.conf` 追加：

```text
plymouth.enable=0
```

保留：

```text
maxcpus=4
```

备份：

```text
/extlinux/extlinux.conf.before_plymouth_off_20260618_093828
```

### ROOTFS systemd

已 mask plymouth 相关单元：

- `plymouth-start.service`
- `plymouth-quit.service`
- `plymouth-quit-wait.service`
- `plymouth-read-write.service`
- `plymouth-switch-root.service`
- `plymouth-reboot.service`
- `plymouth-poweroff.service`
- `plymouth-halt.service`
- `systemd-ask-password-plymouth.path`

已恢复：

- `systemd-udev-trigger.service`

已清理错误串口：

- mask `serial-getty@ttyAML0.service`

已确保：

- `serial-getty@ttyS2.service` root autologin

为降低首次启动噪声，额外 mask：

- `apt-daily.timer`
- `apt-daily-upgrade.timer`
- `dpkg-db-backup.timer`
- `man-db.timer`
- `unattended-upgrades.service`

## 3. 下一轮预期

应不再卡在 `plymouth-quit-wait.service`，并应出现：

```text
armbian login: root (automatic login)
```

或至少 `serial-getty@ttyS2.service` 启动成功。
