# 6.18.33 GMAC clock_in_out=output 测试应用记录

日期: 2026-06-18
目的: 测试 GMAC 时钟方向对 DMA reset failed 的影响。

## 新 DTB

```text
/mnt/sdboot/dtb/rockchip/gmac-clockout-test.dtb
```

来源：

```text
/mnt/sdboot/dtb/rockchip/gmac-pc0-test.dtb
```

## 修改

```text
clock_in_out = "output"
```

验证：

```text
fdtget -t s /mnt/sdboot/dtb/rockchip/gmac-clockout-test.dtb /ethernet@fe300000 clock_in_out
output
```

## extlinux

已切换：

```text
FDT /dtb/rockchip/gmac-clockout-test.dtb
```

备份：

```text
/mnt/sdboot/extlinux/extlinux.conf.gmac-clockout-test.bak
```
